{
  "Application use case": {
    "scope": "rust",
    "prefix": "usecase",
    "description": "Application-layer use case: Command + Output + run over the module's Ports struct (DD 55836674, amended 2026-09-04)",
    "body": [
      "#[derive(Debug, Clone, PartialEq, Eq)]",
      "pub struct Command {",
      "    $2",
      "}",
      "#[derive(Debug, Clone, PartialEq, Eq)]",
      "pub struct Output;",
      "",
      "impl $3<'_> {",
      "    pub async fn $TM_FILENAME_BASE(",
      "        &self,",
      "        cmd: Command,",
      "    ) -> $1<Output> {",
      "        let ports = self.ports();",
      "        let Command { } = cmd;",
      "        $0",
      "    }",
      "}"
    ]
  },
  "Unit of work: begin": {
    "scope": "rust",
    "prefix": "tbegin",
    "description": "Open a unit of work on the orchestrator's database port",
    "body": [
      "let mut uow = ${1:ports}.database.begin().await?;"
    ]
  },
  "Unit of work: commit": {
    "scope": "rust",
    "prefix": "tcommit",
    "description": "Commit the unit of work (consumes it)",
    "body": [
      "uow.commit().await?;"
    ]
  },
  "Unit of work: rollback": {
    "scope": "rust",
    "prefix": "trollback",
    "description": "Roll the unit of work back explicitly (drop does the same)",
    "body": [
      "uow.rollback().await?;"
    ]
  },
  "Struct": {
    "scope": "rust",
    "prefix": "struct",
    "description": "Create a full struct",
    "body": [
      "#[derive(Debug, Clone, PartialEq, Eq)]",
      "pub struct $0"
    ]
  }
}
