# Logbook — uow `8ef12a`

- **Unit:** ZMVP-50 — Signed `did:plc` UPDATE op + retryable outbox (reusable; unblocks ZMVP-46).
- **Ledger:** `.understand/parallel-set.json` · **Briefing:** `.understand/20260701-224305_ZMVP-50_signed-plc-update-outbox.md`
- **DDs:** 26804226 (custody/crypto), 27852802 (handle-change flow), 24150017 (no cross-store txn), 26935298 (identity-only v1).

## Open threads (what's not yet sound)

- [x] **Build classifier risk — did NOT fire.** Fable/`myth` completed the secp256k1-signing build without refusing (framed as a mechanical mirror of the shipped tombstone path). Data point for the model-policy reconcile: Fable's bar seems to be on security *review/analysis*, not a crypto *build* framed mechanically. — resolved 2026-07-01T22:xx.
- [x] **`/security-review` on Opus — DONE** (independent designer). 3 findings: F1 (chain fork, HIGH), F2 (key hygiene, MEDIUM), F3 (resubmit idempotency, LOW/launch-gated). Cleared: handle-trust, byte-exactness/low-S/determinism, benign-replay guard, no key leak, no cross-store txn.
- [x] **F1 FIXED in-ticket** (Engineer approved): partial `UNIQUE(did, prev) WHERE prev IS NOT NULL` migration (20260702000000) makes a non-genesis fork unrepresentable; `update_handle`'s guard propagates the loser's rejection → caller retries onto the new tip (linear serialization). Tests: `a_forking_update_is_rejected_and_the_chain_stays_linear` (adapter), `two_different_ops_cannot_chain_the_same_prev` (pg).
- [x] **F2 FIXED in-ticket** (Engineer approved): `update_handle` now reads the prior op via new `PlcOperationLog::latest_op`, carries public `rotationKeys`/`verificationMethods` forward from its JSON, and imports ONLY the operational key — cold-recovery/signing private scalars are never decrypted into a keypair for a routine update. Test: `update_uses_only_the_operational_key_carrying_public_fields_from_the_log`. NOTE: the single-blob envelope still decrypts all three into `AccountKeys` at `key_store.get`; per-key envelopes would remove even that transient — out of scope, worth a future note.
- [ ] **F3 (launch-gated) — track:** submit-before-record means a retry may re-submit an identical already-applied op to the canonical directory; self-heals only if plc.directory treats resubmit as idempotent. Shared with `tombstone` (not a regression). Verify against the real directory API before launch (ZMVP-51/launch checklist).
- [ ] **`alsoKnownAs` REPLACE must not clobber other DID-doc state.** v1 reconstructs the full op from stored keys (rotationKeys, verificationMethods, empty services) since we don't fetch the current doc from the directory. Safe *only while* identity-only v1 holds no untracked services/keys. Assert the reconstructed non-aka fields equal the genesis shape.
- [ ] **Idempotency rests on deterministic signing** (verified: atrium-crypto `try_sign`=RFC6979 + `normalize_s`). §6 has a determinism test that *enforces* it — if it ever fails, the content-address idempotency model breaks and needs an explicit idempotency key.
- [ ] **Model-policy tension to reconcile.** Ledger cites `feedback_model_split_fable_impl_opus_design` (impl→Fable, security-review→Opus); MEMORY.md index cites `feedback_model_assignment_policy` (Sonnet builds, Fable opt-in-only-for-hardest-non-security-builds, barred from security). Engineer's choice here (Fable build + Opus review) matches the former. Reconcile the two memories post-unit.

## Entries

- **2026-07-01T22:43** — `/understand` produced (Opus). Design fully settled by DDs; scope narrowed to op-type + verb + idempotent/replayable contract, reusing existing tombstone crypto/log path. ZMVP-49 (minter, #83) provides all substrate (`plc.rs` builders, `KeyStore`, `PlcOperationLog::latest_cid`, gated `PlcDirectory`).
- **2026-07-01T22:47** — Verified from source that `atrium-crypto` 0.1.3 signing is deterministic (RFC 6979 + low-S) → idempotency = content-address + existing `UNIQUE(cid)`. No new idempotency key. `prev` from our own log; submission gated no-op in v1.
- **2026-07-01T22:48** — Engineer disposed the two forks: **A1** (contract-only outbox, no worker), **B1** (extend `DidMinter`). Ownership: **Claude builds on Fable 5**, `/security-review` on **Opus 4.8** after (overrode my Claude-on-Opus rec).
- **2026-07-01T22:49** — uow `8ef12a` minted; ledger + logbook recorded. Pre-gate: single ticket, no cross-ticket seams; drift/invariants covered in the briefing → clean. `/start`: worktree `~/code/zurfur-zmvp-50-plc-update` off main, Jira → In Progress. Fable `/implement` spawned.
- **2026-07-01T23:1x** — Fable build complete; **gate verified GREEN by conductor** in worktree (fmt clean, clippy -D warnings 0, pinned genesis vectors unchanged, `signing_is_deterministic` + 12 new update tests + PG testcontainers round-trip/dup-rejection all pass). Design taken: generalized `GenesisOperation`→`PlcOperation{prev: Option<String>}`, ONE DAG-CBOR path (genesis=None, update=Some); `DidMinter::update_handle(did, handle)` mirrors tombstone (submit-before-record; append-fail re-checks latest_cid==our cid → benign-dup success). No migration, no new SQL, `.sqlx` untouched. LSP "Connection refused"/unused-import diagnostics = editor-without-DB noise (fired on unmodified key_store.rs), not real.
- **2026-07-01T22:5x** — Engineer chose **faithful primitive** (Fork 1a): a same-handle re-call appends a valid op; churn-skip is ZMVP-46's policy. No code change (already built that way); documented for the consumer.
- **2026-07-01T23:0x** — `/critique` clean (one byte-exact path, no reinvention). `/security-review` (independent Opus designer): F1 HIGH chain-fork + F2 MEDIUM key-hygiene + F3 LOW launch-gated. Engineer chose **fix F1 & F2 in-ticket**.
- **2026-07-01T23:3x** — F1+F2 implemented by conductor (Opus, security lane): migration `20260702000000` partial `UNIQUE(did, prev)`; `PlcOperationLog::latest_op`; `update_handle` carries public fields forward + imports only operational key. `cargo sqlx prepare` (new latest_op query) committed. **Gate re-verified GREEN: 210 passed / 0 failed**, fmt+clippy clean. Collapsible-if → let-chains to satisfy clippy.
- **2026-07-01T23:5x** — Committed `8e2ad14` `[ZMVP-50][uow:8ef12a]`; **PR [#89](https://github.com/ZurNetwork/ZurFur/pull/89)** open → main (base 91b5114, clean replay); Copilot review requested; Jira → In Review.
- **2026-07-02** — `/address-comments`: Copilot left 2 inline comments. **#1 (security path, FIXED):** update_handle reconstructed from rotationKeys+atproto only → could silently drop `services`/extra VMs or confusingly error on a tombstone. Added an identity-only guard (reject non-`plc_operation` prior + non-identity-only shape, fail LOUD) + 2 tests. **#2 (nit, FIXED):** doc tense. Re-gate GREEN (212 passed); pushed ff `8e2ad14..74c270c`; replied on both threads. One pass.
- **2026-07-02** — **MERGED** → main `f3268bc` (#89). Integrate: worktree Docker stack down, worktree + local branch removed, local main ff to f3268bc. ZMVP-50 → **Done**. **ZMVP-46 unblocked** (Engineer's lane, In Progress). Unit `8ef12a` **COMPLETE**.

## Close-out follow-ups (carried out of the unit)
- **F3 (launch-gated):** verify `plc.directory` treats a resubmit of an already-applied op as idempotent before real submission (shared with tombstone). Not filed as a ticket yet — offered to Engineer.
- **Model-policy reconcile — RESOLVED 2026-07-02 (/optimize-memory).** `feedback_model_split_fable_impl_opus_design` was a PHANTOM (no such file). Engineer confirmed ZMVP-50's Fable-build = a ONE-OFF explicit override; `feedback_model_assignment_policy` STANDS unchanged (Sonnet default builder, Fable opt-in non-security only, Opus all security+review). Softened the memory's overstated "hard-refuses security-adjacent" → framing-sensitive (mechanically-framed build passed; review/novel-security still refuses); recorded the one-off + "don't treat as precedent."
