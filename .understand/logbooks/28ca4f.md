# Logbook — uow `28ca4f` · Epic ZMVP-101 "The Twenty One"

Atproto record write-path against a local, wipeable PDS. Worked in dependency **waves**.
Ledger: `.understand/parallel-set.json` · Tickets: ZMVP-102/103/104 (Wave 1), 105 (W2), 106 (W3), 107 (W4, optional).

## Open threads (what's not yet sound — do not lose)

- ⚠️ **ALL Wave-1 Engineer forks RESOLVED via interview (2026-07-04).** Decisions recorded in ledger `decisions{}`. Cadence=continuous; 104 scope=feed.post-only; 102=standalone reference-PDS(+dev-env fallback); 103=new `backend/crates/test-support/` crate + extensible acting-credential seam; image-pin=canonical `ZURFUR_PDS_IMAGE`+equality `#[test]`; blocking-gaps row=CLOSE; feed.post field set decided (see below).
- ⚠️ **feed.post SPEC SUPERSEDED (2026-07-04)** — the old interview spec (blob/title/description) is dead; the record is now the **Replyable-unified** post/reply/shout (required createdAt+labels; optional text/embed/reply/credits/snapshot). See ledger `feed_post_field_set_UNIFIED`. All the old feed.post open threads below this line (medium/commissionRef/single-blob/design-sync-pending) are RETIRED — the Engineer finalized the pages tonight and Claude applied the D2 tense fix.
- ⚠️ **`snapshot` shape is an OPEN Engineer decision** — DD §9 lists it (optional) with no shape; 104 OMITS it as additive-later. Author its shape + add to the lexicon when a feature needs it. Not blocking this epic.
- ⚠️ **`text` maxGraphemes cap = 3000 by default** (mirrors the description ceiling) — FLAGGED for Engineer ratification. And **`labels` required + Safe=empty-values** encoding — confirm it reads right.
- ⚠️ **`credits` (DIDs) on-record** stays a boundary/correlation surface for the epic's /security-review; consent is the moderation road (render-side blocks + Index suppression), not schema.
- ⚠️ **102 beyond-brief, needs an Engineer nod**: it AUTHORED a local `did:plc` container (`docker/plc/Dockerfile`, @did-plc/server npm 0.0.1, in-memory mock) — real ongoing-maintenance artifact — and added a 5th worktree port. 103 independently built its own in-process StubPlc for tests. Proceed unless Engineer objects.
- ⚠️ **/close-gaps --post MUST confirm the 102↔103 PDS image literal match** — 103's drift `#[test]` is UNARMED until 102's `ZURFUR_PDS_IMAGE` lands in `.env.example` (102 unmerged). Digest currently mirrored: `ghcr.io/bluesky-social/pds@sha256:1fa8bbce…`.
- ⚠️ **For the Opus /security-review** (102 + 103 + eventual 105): 102 network `internal:true` egress-block (AC4); 103 StubPlc binds `0.0.0.0` (ephemeral, test-lifetime); credits DID exposure; the fixture PDS session token handling. host-gateway CI assumption proven by first PR run.
- ⚠️ **Blocking-Gaps row (9994307) closure DEFERRED until the ZMVP-104 PR MERGES to main** — the draft (worktree `.understand/104-blocking-gaps-closure-draft.md`) says "the JSON landed," but it's only on the branch. Closing now = the same premature-present-tense issue D2 fixed. EXECUTE the closure (check box + swap "closes when the JSON lands" → closure line) as part of 104's post-merge integration, NOT before. Also flagged there: the page's bottom "last blocking row is transcription work" summary goes stale once closed — optional Engineer reword.
- ⚠️ **FOLLOW-UP: verify the dev loop on Docker-proper.** 102's `internal:true` network + published host ports is verified on podman/netavark (the house provider) but Docker-proper may NOT DNAT host ports on an internal network → `just up` could fail for a Docker user. Engineer chose (a): downgrade the compose comment now + flag this follow-up (not chase Docker compat now). Not tracked as a Jira ticket yet — offer to file one if the Engineer wants it formal.
- **2026-07-04 ~evening — Review passes complete.** (1) `/address-comments`: 7 Copilot comments across #94/#96 — 4 declined false-positives (json!-move ×3, std::Mutex), 3 fixed (doctest name; smoke `down -v`; AC4 static grep→pds_net awk), all threads resolved. (2) Fable-5 LOGIC-only review (Engineer opt-in, security fenced out): no blockers, 104 clean; 2 false-green assertions fixed+verified — 103 teardown (`is_connect()`, 3 tests green) and 102 AC4 runtime probe (raw-TCP node probe + positive control, `just pds-smoke` all-pass). New heads: 102 `9a30518`, 103 `4469828`. (3) Haiku doing the last 2 should-fixes on 102 (compose comment downgrade + worktree-init re-run secret seeding). Nits deferred (6, low-value). After Haiku pushes: PRs ready for Engineer merge.
- ⚠️ **SHIP phase in flight (2026-07-04 ~evening)**: 3 Opus ship agents running per-worktree — 102 (abbac1309), 103 (aeb431487), 104 (ae37ba28) — each: /security-review → apply clear fixes / STOP+surface if judgment needed → gate green → /prepare-pr (first commit `[ZMVP-N][uow:28ca4f]`, exclude `.understand/`) → PR opens → STOP for Engineer merge. Post-gate was CLEAR (no blockers). On each merge: sync main, `git worktree remove`, ledger→merged. Cadence: Engineer merges (Claude never self-merges).
- **ZMVP-105 auth fork** (deferred to that ticket): Jacquard OAuth vs PDS local creds for dev writes — where any PgAuthStore remainder from The Who would surface. In-ticket Engineer call. 105 also = MANDATORY Opus /security-review (crosses the data boundary).
- **feed.comment is hard-blocked** — no Comments DD exists; cannot be finalized in 104 regardless of scope choice.
- **Doc drift to fix during 104 design-sync** (not now): Lexicon page 10354710 still shows the OLD feed.post shape (text/embed/kind/rating/tags) + old rating token — superseded by Units 3+4. Registry 29818896 must gain "repo JSON canonical" + mirror final fields. Close last row of Blocking Gaps 9994307.
- **`docs/confluence-design-index.md` stale by 8 pages** (Boundary-Contract + Seals programme): 29818896, 29622283, 29687820, 29949954, 29982722, 29622362, 29622321, 30507148. Fold into 104's /design-sync (or a /save-reference sweep).
- **Image-pin single-source-of-truth** seam between 102 (docker-compose.yml) and 103 (Rust testcontainers) — pin the PDS image tag in ONE place to avoid drift. Catch at /close-gaps --pre.
- Note: CLAUDE.md references a `config-and-runtime` memory that does **not** exist on disk; 102 briefing grounded config facts directly in Justfile/compose/.env.example/dev.toml instead. (Possible memory-map cleanup item.)

## Entries

- **2026-07-05 ~17:40 — Retrospective written** → `.understand/retrospectives/retrospective-2026-07-05-28ca4f.md`. Headline lesson (→ memory `feedback_review_specs_against_threat`): the AC4 `$type` gap survived briefing+build+security review because all stages verified against the conductor's own spec — cold-reading Copilot caught it; one threat-first reviewer per pipeline from now on. Unit review tally corrected: 12 top-level external comments across #94–#98 → 8 real (fixed) + 4 false positives (declined with reasons).

- **2026-07-05 ~17:20 — EPIC ZMVP-101 CLOSED · UNIT 28ca4f COMPLETE.** Engineer merged #98 (→ main `798a6cf`) and re-affirmed delegation ("keep going until the Epic is done"). Integrated 106 (main synced, worktree/branch removed, Jira Done). **Ruled 107 SKIP-AND-CLOSE** (the unit's last delegated call; reopening the ticket = the veto): the ticket's own rule is "skip-and-close if the local rig already answered everything" — 106's empirical determinism against the digest-pinned upstream PDS image met the epic's exit criterion in full, and by the pull rule nothing else was the epic's question. The 3 named unknowns re-homed rather than dropped: OAuth-vs-real-Bluesky → publish/compose feature (DD 29687820 + ZMVP-108; no OAuth credential arm exists to validate today); PLC directory → identity tickets (ZMVP-51/53); latency/rate-limits → first real-network feature. Anti-consideration recorded: a burner would put pre-publication lexicon shapes in public repos (against publish-late 29818896). Jira: 107 Done w/ ruling comment, **epic ZMVP-101 Done** w/ closing summary. Coherence check: Lexicon page 10354710 has NO stale ZMVP-105/106 rule annotations (clean); known nuance stands (page lists optional `snapshot`, repo JSON omits additive-later — the open Engineer flag). Unit tally: 5 PRs merged (#94-#98), 2 epics' worth of review rounds all clean (5 Opus security reviews CLEAN, 8 Copilot comments → 8 real → 8 fixed... correction: 102/103 round was 7 comments/3 fixed/4 declined false-positives; 105+106 rounds 5/5 real fixed), ZMVP-108 carved, 0 open threads. **No in-flight work remains.**

- **2026-07-05 ~17:00 — /address-comments 98 (ZMVP-106) — external review loop CLOSED.** Copilot: 3 inline, all verified REAL, all FIXED in `12bdadd` + replied + resolved. The substantive one: **the AC4 guard excluded `$type` from checking entirely** — a same-shaped record under a draft `$type` (`app.zurfur.feed.post.draft`) would have passed the tripwire, which is literally AC4's "no draft fields" concern; `stored_value_within` now asserts `$type` == the lexicon's on-disk `id` (pin travels with the file) + a wrong-type rejection case in the fast test; confirmed the adapter emits `$type` (serde rename + default) before wiring the in-flow assert. The other two: `error_for_status()` on both raw helpers (diagnosability). Re-gate GREEN (one rustfmt reflow; clippy clean; `cargo test --workspace` 0 FAILED — capstone re-ran its 2 PDS boots with the strengthened guard against real stored records). Push reconciled `32f63f0→12bdadd` lease-keyed. Single pass; no re-request of Copilot; CI watcher armed on the new head. **Ball: Engineer merges #98** → integrate → rule 107.

- **2026-07-05 ~16:30 — 106 BUILT GREEN + SECURITY-CLEAN + PR #98 OPEN.** Opus build (3 commits, first `e08069e [ZMVP-106][uow:28ca4f]`; red-first evidence for W2+W1): capstone `wipe_replay.rs` (both tests green — `post_with_blob_survives_wipe_and_replay` + `stored_record_has_no_field_outside_the_final_lexicon`, 5.01s), **determinism EMPIRICALLY held** (record_cid + blob_cid equal across two fully separate PDS instances; assert_ne uri held), pub `fixed_created_at` (W3), 3 ZMVP-108 doc redirects (W4, grep-verified 0 stale). Opus /security-review **CLEAN** (all 6 checks PASS, file:line-cited; 1 INFO forward-looking: AC4 top-level-keys guard is sound while wire structs stay closed — recursive check if one ever gains flatten/open-map). No fixes → no re-gate (tree byte-identical to gated build; CI authoritative). PR #98 opened w/ ACs→tests + delegated rulings + Engineer flags (AC4 exact-pin tripwire; credits=[] not exercised here — rides 105 coverage; builder noted commit trailer says Fable per env template though Opus built, cosmetic). Copilot requested; Jira 106 → In Review; CI watcher armed. **Ball: Engineer merges #98.** Then: integrate → rule **107 run-or-skip** (evidence so far: green + high confidence → leaning skip-and-close-ZMVP-101; Engineer can steer).

- **2026-07-05 ~15:45 — 106 BRIEFED, ALL 6 FORKS RULED (delegation), ZMVP-108 CARVED, BUILD LAUNCHED.** Opus briefing (snapshot `20260705-153613_ZMVP-106_wipe-replay-capstone.md`) headline: **105's contract suite already proves AC1 single-run** (pinned `fixed_created_at`, field-identical read-back, byte-identical getBlob) → 106's real delta = cross-wipe determinism + mechanical AC4. It also VERIFIED (atproto Repository spec, cited) record-CID = deterministic DAG-CBOR content hash vs per-instance commit-CID/rev — making CID-equality-across-wipes a sound pass condition. Conductor rulings (each vetoable): **F1** re-defer the 4 publish rules → **created ZMVP-108** as the recorded owner (no compose layer exists; maturity label underivable without rating context per 29982722 §4; 106 corrects the 3 stale `(ZMVP-106)` doc-comments to cite it; linked Relates to 105+106); **F2** publish violations never enter `PublicRecordsError::InvalidRecord` (XRPC-outcome classifier by contract; ledger's 'no producer' note was imprecise — structural jac_* producers exist); **F3** `adapter-atproto/tests/wipe_replay.rs`; **F4** wipe = fresh ThrowawayPds per run, one test body; **F5** layered asserts incl. cross-run record-CID equality, `assert_ne` on uri, never rev/commit; **F6** AC4 mechanical (stored keys ⊆ lexicon properties + required present) — overturns the 'by review only' ledger assumption. **Opus build agent launched** (W3→W2→W1 red-first + W4, gates, self-critique, docs, commits `[ZMVP-106][uow:28ca4f]`, stops before PR). Next: Opus /security-review (mandatory, light) → ship. 107 ruled AFTER 106's evidence.

- **2026-07-05 ~15:30 — 105 INTEGRATED (Engineer merged #97 → main `342a7a1`) + WAVE 3 OPENED under re-affirmed delegation.** Engineer: "Keep going. Remember, for THIS EPIC ONLY, you can make domain decisions." Integration: main synced (autostashed ledger edit around pull), 105 worktree+branch removed (remote auto-deleted, no compose stack/volumes existed), briefing snapshot preserved to primary `.understand/`, Jira 105 → Done. **Stale-TODO finding:** the 2 ticket factual corrections were ALREADY in the 105 description (a "Correction 2026-07-05" clause — existing-ports premise + no-PgAuthStore both fixed); verified rather than re-applied. Wave 3: worktree `zurfur-zmvp-106-wipe-replay-capstone` created (ports 33698–33702), Jira 106 → In Progress, **Opus /understand launched** with 6 named forks to ground (F1 publish-rules ownership — 105 deferred 4 app-side rules to 106 but the ticket says "deliberately thin"; F2 InvalidRecord producer; F3 test home; F4 wipe mechanics; F5 determinism/CID-equality soundness; F6 AC4 mechanical subset). Conductor will rule the forks transparently under the epic delegation, then launch the build.

- **2026-07-05 ~15:05 — /address-comments 97 (ZMVP-105) — external review loop CLOSED.** (Bridging note: the 5 forks below were DELEGATED same-day — Engineer granted epic-scoped decision authority, "do what you think is best" — Opus built to green, Opus /security-review CLEAN, /prepare-pr opened #97; details in ledger 105 fields.) Copilot: 2 inline comments, both verified REAL against the code, both FIXED in `05f2666` + replied + threads resolved: (1) `upload_blob` returned the request byte-length as `BlobRef.size` while the port documents the repo-recorded size — now parses response `blob.size`, request-size fallback mirroring `mimeType`; (2) `AtUri::parse` silently swallowed `?query`/`#fragment` into the rkey despite the doc-promised repo-record form (`Nsid`/`RecordKey` are non-validating newtypes, nothing downstream caught it) — now rejects `?`/`#` after the scheme; 2 regression tests added. Re-gate GREEN in the 105 worktree (fmt; clippy -D warnings; `cargo test --workspace` 0 fail — new test ran, `uploaded_blob_downloads_byte_identical` exercised the new parse against the live PDS). Push reconciled `5aff021→05f2666`, lease keyed to the fetched oid. Single pass per skill — no re-request of Copilot. **Ball: Engineer merges #97.** Outstanding 105 chore: correct the 2 Jira ticket factual errors (existing-ports premise; AtprotoAuthStore/ZMVP-12 not PgAuthStore/ZMVP-7).

- **2026-07-05 ~00:20 — WAVE 1 CLOSED + WAVE 2 (105) BLOCKED on 5 Engineer forks.** Wave 1 all merged (102 #96, 103 #94, 104 #95 → main dd2e920), worktrees/branches cleaned, Blocking-Gaps 9994307 CLOSED (v4). Review passes done (Copilot triage + Opus /security-review + Fable-5 logic pass, 2 false-greens fixed). Opened 105: `/start`+`/understand` (Opus, worktree `zurfur-zmvp-105-atproto-record-writes`). Briefing surfaced the ticket premise is WRONG — **no record-write domain port exists** (13 traits, all identity/profile), so 105 must DEFINE a new `PublicRecords` port + value types (fresh domain modeling, no DD fixes the shape) = Engineer's lane. 5 forks put to Engineer (awaiting): (A+D) port+types ownership, (B) auth Bearer-vs-OAuth [rec Bearer], (C) scope defer-publish-rules-to-106 [rec defer], (Error) typed-vs-anyhow [rec typed]. Ticket errors flagged (no PgAuthStore → it's AtprotoAuthStore ZMVP-12; "existing ports" false). Per roles, PAUSED — did not guess any fork. Ledger 105 = `blocked`; briefing snapshot in the 105 worktree. **106 (capstone) blocked on 105; 107 optional after 106.**

- **2026-07-04 ~14:45** — uow `28ca4f` opened for epic ZMVP-101. Invoked as `/unit-of-work ZMVP-101 Epic. Most of this is boilerplate. Parallelize as much as possible`. Ledger 8ef12a was clean (no in-flight unit). Expanded epic → 6 children; mapped dep waves (102/103/104 ∥ → 105 → 106 → [107]).
- **2026-07-04 ~14:50** — Fanned out **3 parallel `/understand` agents (Opus/designer)** on the Wave-1 set. Snapshots saved under `.understand/`. Key finding that revises the "most is boilerplate" framing: **both 102 and 103 briefings recommend Engineer-led ownership** on the forky pieces (PDS topology, hermeticity, fixture-seam) with Claude on the mechanical plumbing — not pure boilerplate. **104 confirmed Engineer-domain**, and collapses (per epic pull-rule) to **finalize `feed.post` only** — the sole record this epic ships, fields already ruled in Gallery Posts DD 29949954 §9.
- **2026-07-04 ~15:05** — Batched all Engineer calls into 4 questions (twice; user AFK/slow — engaged but not yet answered). Per roles model, **paused rather than guessing any fork**. Recorded ledger (all 6 tickets, phases: 102/103/104 `blocked`-on-decision, 105/106/107 `planned`) + this logbook. Next: on answer → /parallelize Claude set → /close-gaps --pre → build.
- **2026-07-04 ~15:20** — Engineer returned: **"This epic is all yours. Interview me."** → Claude now IMPLEMENTS all tickets (104/107 included), Engineer disposes forks via interview. Ran `/close-gaps --pre` (background, designer): **no blocking ticket to carve**, lanes file-disjoint; corrected crate path to `backend/crates/test-support/` (G-CRATE, workspace glob); flagged credits/commissionRef boundary invariants; routed image-pin mechanism + blocking-gaps-row + commissionRef-generation to Engineer.
- **2026-07-04 ~15:30** — Interview rounds 1–3. Fetched Gallery Posts DD (29949954 §9 field list) + Maturity Vocab (29982722) in HTML (tables didn't render as markdown). Engineer decided: cadence=continuous; 104=feed.post-only; topology=standalone reference-PDS; harness=new test-support crate; blocking-gaps row=CLOSE; blob=single visual (img 10MB / video mp4+webm 100MB, alt required); credits=`[{subject,role}]`; medium=open-string knownValues; text caps 128/3000 gr. **Field-set crux**: Engineer initially leaned minimal (maturity+blob+createdAt). Claude pushed back (argue-then-defer) — dropping credits is a privacy win, dropping title/description isn't (poster's low-risk speech + the record's reason to exist). Re-framed in plain language when Engineer said the jargon was unclear. **Final: caption ON-record, credits ON-record** (Engineer overrode Claude's privacy caution on credits — deferred), medium/commissionRef/tags Index-side. Fable-5-on-hardest reconciled: 103→Fable (Opus review), 102→Sonnet, 104→Opus (security-nature, Fable barred).
- **2026-07-04 ~15:45** — Recorded all decisions to ledger `decisions{}`; phases 102/103/104 → `implementing`. **Launched Wave-1 build in parallel**: 102 (builder/Sonnet), 103 (myth/Fable 5), 104 (designer/Opus — author lexicon JSON + draft design-sync, no Confluence write yet). Build agents STOP at green `/implement`+`/critique`+`/document` (no /prepare-pr) — Opus /security-review + ship orchestrated after the `/close-gaps --post` cross-set barrier.
- **2026-07-04 ~evening — CORPUS CHANGED UNDER THE UNIT.** Engineer ruled the **Replyable unification** in parallel: `feed.post` becomes the unified post/reply/shout record, `app.zurfur.feed.comment` DELETED, `Commentable`→`Replyable`. This **superseded my interview's `feed.post` spec** (blob/title/description). Confirmed via a read-only Jira+Confluence audit (Lexicon 10354710→v4, Gallery §9 29949954→v3, Replyable DD 30572573). Corpus is self-consistent; audit found only D1 (104 scope overreach — Engineer fixed, narrowed to feed.post-only) + D2 (pages present-tensed repo JSON). **Killed the 104 agent** mid-flight (it independently caught the same divergence; wrote nothing to the corpus).
- **2026-07-04 — D2 closed by Claude** on Engineer's explicit instruction: softened the two "lives in the repo" present-tense claims (Lexicon page v4, Gallery §9 v3) to designate repo canonical without implying the file exists. Full-body replace, verified byte-identical except the one clause each; ZMVP-104's own "repo is source of truth" line left untouched.
- **2026-07-04 ~16:30 — Resumed unit.** 102 = BUILT green (uncommitted; +local-PLC container + 5th port beyond brief, flagged). 103 = BUILT green + committed (3 commits, uow-stamped; test-support crate + StubPlc + FixtureAccount seam). 104 = REDO: corrected ledger `feed_post_field_set_UNIFIED`, **relaunched 104 (designer/Opus)** to author the unified feed.post JSON (reuse the @atproto/lexicon harness, discard stale JSON, OMIT snapshot as additive-later, default text 3000gr, labels-required selfLabels Safe=empty, +8 index pages, DRAFT blocking-gaps closure). Next after 104 green: `/close-gaps --post` (must confirm 102↔103 image-literal match) → ship each (Opus /security-review → /prepare-pr).
