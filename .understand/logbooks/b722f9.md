# Unit of work `b722f9` — ZMVP-26 ∥ ZMVP-38

**Set:** ZMVP-26 (OpenAPI `/plugin/v1` spec) ∥ ZMVP-38 (Collection lexicon). Both Claude-owned, disjoint footprints.
**Minted:** 2026-06-30. Planning carried over from handoff-20260628.md (prior planning session).
**Ledger:** `.understand/parallel-set.json` · **Prior handoff:** `.understand/logbooks/handoff-20260628.md`

## Open threads (what's not yet sound / loose ends)
- [ ] **Build the set:** ZMVP-26 (OpenAPI spec file) ∥ ZMVP-38 (Collection lexicon). Disjoint; neither touches parked ZMVP-39 (router) or ZMVP-36 (UoW).
- [ ] **ZMVP-26:** author the settled surface (auth = app_key bearer, errors = RFC 9457, versioning, CSRF-exempt). **Stub Commission resource bodies as `$ref`s pending ZMVP-19** (Commission schema not pinned). Confirm stub approach at ship gate before the PR opens.
- [ ] **ZMVP-38:** Referenceable v1 = **Account + User only** (decided, on Collection DD 24182787). First-of-kind lexicon — no `lexicons/` dir yet; the convention is part of the work. Character/Post are a later backward-compatible enum addition.
- [x] **Stale Jira statuses** — RESOLVED 2026-06-30: ZMVP-21/23/24/40 all now Done (Engineer moved 21/23/40; 24 also Done). Verified via JQL refresh.
- [ ] **DD 24870914 open items** (not this unit): Zurfur reserved-label list; punycode/confusable policy; Account-handle change flow vs user handle-swap.

## Decisions carried in (recorded in Confluence)
1. Default Account handle is user-chosen at onboarding → DD 24870914.
2. Referenceable v1 = Account + User only → Collection DD 24182787.

## Decisions made this session (2026-06-30, pre-gate)
The handoff's "all forks settled" was an overclaim; a fresh Confluence read (DD 24543244, Collection DD 24182787, Lexicon 10354710, Plugin 3047451, Lens 10453000) found genuine open Engineer forks. Surfaced 4; Engineer decided all (recommended options):
3. **ZMVP-26 = spec-first standalone OpenAPI doc** (not code-first utoipa; the openapi-infra branch's code-first tooling is set aside for this ticket — the ticket is spec-only/no-routes). File: `openapi/plugin-v1.yaml` (default convention).
4. **ZMVP-26 scope = settled envelope + stubs:** info/servers, app_key bearer (CSRF-exempt), problem+json errors → urn:zurfur:error:* registry, version pinning, + the two MVP-slice operations (read commission; write changelog artifact). Commission bodies = `$ref` stubs pending ZMVP-19; scope tokens from Plugin-page examples, marked PROVISIONAL pending the DD's scope-vocabulary ticket.
5. **ZMVP-38 NSID = `app.zurfur.graph.collection`** (mirrors atproto `app.bsky.graph.list`; graph=membership family). First Zurfur lexicon → sets namespace precedent; lands on Lexicon page 10354710 via design-sync.
6. **ZMVP-38 Dynamic = `defs#lens` ref + ship Static.** All v1 profile collections are Static (DD decision 3), so Dynamic isn't exercised; Lens lexicon deferred to its own DD. File: `lexicons/app.zurfur.graph.collection.json`, JSON-only (no Rust codegen) — "green" = lexicon-validates + Lexicon-page row.

## Open threads (added 2026-06-30)
- [ ] ZMVP-26 scope tokens are PROVISIONAL — reconcile when the DD 24543244 scope-vocabulary ticket lands.
- [ ] ZMVP-26 plugin-surface 401/403 error `detail` copy — the core registry (problem.rs) strings are account/session-surface phrasing ("signed in", "on this account"). Spec examples now use neutral plugin copy + a note. When /plugin/v1 handlers are built (ZMVP-19/handler work), decide whether they reuse the registry errors or get surface-appropriate details, and align. (Surfaced by Copilot review on PR #74, 2026-06-30.)
- [ ] ZMVP-26 Commission/changelog component bodies are `$ref` stubs — fill when ZMVP-19 pins the Index/export schema.
- [ ] ZMVP-38 produces a JSON artifact + validation, not cargo tests — the lifecycle's TDD gate is lighter here (lexicon-validity + design-sync), flag at ship.
- [ ] No domain `Referenceable`/`Collection` Rust primitive exists and no ticket covers it — ZMVP-38 is lexicon-only by decision. A future ticket needs to land the domain trait/struct.
- [x] Record `graph.*` namespace precedent on Lexicon page (10354710) — DONE 2026-06-30 (page v2): added the collection record row, the #lens stub entry, and a family-segment convention line. Engineer-confirmed.
- [ ] ZMVP-26 404/403 + commission-not-found error code — Engineer DECIDED: ship provisional, resolve with ZMVP-19. (Now tracked as a ZMVP-19 follow-up, not an open fork.)

## ✅ UNIT COMPLETE — 2026-06-30
ZMVP-26 (#74) + ZMVP-38 (#73) squash-merged to main (38552a6 / f179274), Jira Done, worktrees + branches removed, main synced. Copilot review on #74 addressed (plugin-surface error copy). Open follow-ups carried below survive into the next unit (ZMVP-19 fills the ZMVP-26 stubs/scope-vocab/404-403/error-detail; domain Referenceable/Collection Rust primitive still unticketed).

## Trail (continued below — see post-gate)
- 2026-06-30 — Both tickets BUILT. ZMVP-38: lexicons/app.zurfur.graph.collection.json + graph.defs.json (#lens stub), validated vs real @atproto/lexicon meta-schema. ZMVP-26: openapi/plugin-v1.yaml (OpenAPI 3.1) + ci.yml spec-lint job, redocly-clean.
- 2026-06-30 — `/close-gaps --post`: ✅ set coherent, no blocking findings. Footprints disjoint (openapi/+ci.yml vs lexicons/). Domain boundary respected (Commission=Private stub; Collection=public record). Verified ZMVP-26 Problem schema matches problem.rs field-for-field (5 fields, no instance/flatten); only commission-not-found is non-registry (correctly flagged provisional). Ride-along items: (1) Engineer 404-vs-403 + error-code reg [coupled to ZMVP-19]; (2) ZMVP-38 design-sync on Lexicon page 10354710.

## Trail
- 2026-06-30 — Both SHIPPED to open PRs (paused for merge): ZMVP-38 → PR #73, ZMVP-26 → PR #74. Gates green; first commits stamped [ZMVP-N][uow:b722f9]. ZMVP-26 /security-review clean (auth-boundary contract: CSRF-exemption scoped, app_key→token correct, no global DID, no private leak). Lexicon page 10354710 design-synced (v2). Copilot review requested on both (#73 came back empty/no findings). Engineer confirmed: keep the membership union shape (static-xor-dynamic; valid atproto, mirrors app.bsky embed union) — no change. Standing pref saved: request Copilot review on every PR.
- 2026-06-30 — Resumed planning-complete unit from handoff-20260628.md. Minted uow `b722f9`. Synced main (origin moved to 040b2de via docs PR #72; switched primary off the merged docs branch onto main).
- 2026-06-30 — Jira: ZMVP-26 + ZMVP-38 assigned to Zuri (acct 70121:e1c093a9…) and transitioned To Do → In Progress (per Engineer request). Fanned out 2 build agents (one worktree each).
- 2026-06-30 — `/close-gaps --pre`: set structurally coherent (disjoint footprints, no domain violation), but BOTH tickets had open forks the handoff missed. Fetched 5 DESIGN pages for ground truth. Surfaced 4 forks → Engineer chose all recommended. Pre-gate RESOLVED; lifting the block, fanning out the build.
