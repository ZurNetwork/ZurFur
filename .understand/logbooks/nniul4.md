# Logbook — uow `nniul4` · Epic ZMVP-18 "The Reason (Commissions)"

34 tickets, stacked branches, bottom-up Engineer review. Ledger: `.understand/parallel-set.json` · Gate A: `.understand/gate-a-zmvp18-nniul4-20260705.md` · Interview rulings: `.understand/decisions-zmvp18-interview-20260705.md`.

## Open threads

- ⚠️ **B1–B9 Engineer holds** (gate-a doc) — Engineer away; each blocks only its tickets. Top: B1 (Commission-Admin ruling vs Structural Authority DD 29425666 — a DD **missing from the local index**, as is Seat Ceilings DD 30277634), B2 (participant persistence — gates S3@76+).
- ⚠️ **View-grant READ enforcement unowned** (B3) — 75 builds the additive seam; the grant term + integration test need an assigned owner. Capstones never exercise grants.
- ⚠️ **ZMVP-65 was merged pre-epic** (43d6112/#93) while Jira sat at To Do — worth asking the Engineer how it merged outside a unit (probably their own lane).
- ⚠️ **Post-S3 duties**: full hard-delete cascade integration test (tree+seats+applications+participants+file entry+blobs); confirm every child migration carried ON DELETE CASCADE (E35); /close-gaps --post owns both.
- ⚠️ **Design-sync queue** in gate-a doc (participant wording, Invoices DD row, B5 reconciliation, stale refs) — on Engineer confirmation only.
- ⚠️ **End-of-epic Opus /security-review** (Engineer directive: ONE review at the end): focus 75 serializer (threat-first cold reviewer per memory), 96 composition, 70 grants, 57 severance + stale-SAFETY rewrite, 88 blob authz+headers, 80 visibility check, handshake races, quota, invoice immutability, E3 404 uniformity.

- ⚠️ **PR #100 — 2 decisions left before merge**: (1) clear_channel concurrent double-append fix (apply 68's rows-affected pattern to set_linked_channel — awaiting nod: fix on-branch pre-merge, or ticket); (2) problem.rs constructor dedupe (my rec: named public constructors + private table/macro; awaiting call). linked_channel RESOLVED (DD 32112642 — pointer stays, merge as built; GH thread closed 2026-07-06). Box::pin softened on ZMVP-111 ("keep it for now" if bounds fail). Post-merge duty: Haiku cascade rebases all children onto main (66/85/71 + descendants), then 57's held push + 76's stacking rebase.
- 📌 **Deferred capability (Engineer ruling 2026-07-06, PR #100 thread 3532078209): plugin Custom changelog events** land with the future plugin epic + a Changelog DD 30408741 amendment (namespace-prefixed `plugin:` tokens; parse() keeps rejecting unknown bare tokens; as_str→Cow; payload stays opaque jsonb). No ticket yet by design.

- ⚠️ **ZMVP-90 PR HELD** — markup writes a `markup_added` changelog entry, and the Eventlog DD's taxonomy-partition ruling ("files, markup on the line", expected imminently) may re-home it to an eventlog source. Branch is local-only (salvaged, rebased, green); do NOT open its PR until the partition ruling lands. Same ruling gates the changelog.rs module-doc reword (deferred from PR #100 by Engineer's "merge as built").
- ⚠️ **Eventlog DD follow-ups pending** (page 32178178 Open table): taxonomy partition, consumers, cursors/ordering, dispute freeze, notes placement — Engineer ruling via Claude Web sessions. Re-fetch the DD before building ANYTHING timeline-shaped. Hash-chain + signed checkpoints (D8) → **ZMVP-114 filed 2026-07-06** (under epic ZMVP-18; security-nature/Opus lane; soft-sequenced after the partition ruling; ⚠️ key collides numerically with PR #114/ZMVP-76 — disambiguate).

## Entries

- **2026-07-06 — Eventlog/Linked-Channel rulings landed (Claude Web); /save-reference + /design-sync run.** NEW Eventlog DD 32178178 (changelog=explicit facts w/ hash chain + signed checkpoints; eventlog=derived view over core-persisted source streams; envelope+fallback contract; stored templates; append-only+supersession) + Linked Channel DD 32112642 DECIDED (pointer stays core as opaque slot — bytes ours, meaning the setter's; **PR #100 merges as built, AC3 stands**; ingestion = plugin Golem's own eventlog source; closes External Chat Tracking placement row as Option-5 mechanism; pointer ≠ binding; single pointer). Changelog DD amended (narrowed), Plugin page re-pointed (production → eventlog source; markup native). Index updated (both DDs + changelog note). /design-sync: code drift verified minimal — no eventlog code exists, 87 unaffected (verified, not assumed); ZMVP-90 held (see open thread); Commission + Golem pages were updated by the Web session mid-sync (freshness rows + DD links match what I'd proposed — near-clobber avoided by the Engineer's interrupt; verify-before-write held).

- **2026-07-06 — PR #100 Engineer review round (/address-comments #100).** Engineer reviewed live: 11 inline comments → all answered/triaged on-thread; Copilot leftovers closed (2 already fixed in b91f933, sqlx false-positive declined+resolved with .sqlx-cache proof). **Rulings executed:** new refactor epic **ZMVP-109** created ("after this one", per Engineer) with **ZMVP-111** (UoW ergonomics — AppState::transaction + async closures, no-Box::pin is a HARD requirement), **ZMVP-112** (Authed FromRequestParts extractor sweep), **ZMVP-113** (StringRules explicit-rule validation builder — Engineer approved the counter-proposed shape over the negate-flag sketch, "now required"); **ZMVP-110** (epic-less: endpoint span timings, aggregation only on evidence). ⚠️ Jira keys ZMVP-110..113 numerically collide with PR #110–#113 — always disambiguate. Plugin Custom-events deferred (see open thread note). Cascade state at the interrupt: everything pushed through the stack EXCEPT 57 (rebased+green locally, push held — Engineer went branch-by-branch) and 76 (needs conflicted stacking rebase onto 77, same 68-style sibling defect).

- **2026-07-05 ~20:30 — WAVE A SHIPPED: S0 spine done.** ZMVP-67 → **PR #99** (base main): Fact trait + UoW commission_has_facts (E17/E18), domain module split as pure staged renames, LIVE-proven tripwires (const compile-guard + pg_constraint schema scan with positive control), red-first evidence, critique CLEAN. ZMVP-87 → **PR #100** (base 67): full 34-variant frozen taxonomy verified against the fetched Changelog DD, ChangelogWrites on the UoW + append-only BEFORE UPDATE trigger (beyond-ruling, drop-if-unwanted flag), CommissionStore + owner-arm is_participant, uniform-404 helper (byte-equality tested), notes/channel routes, 'created' genesis emitter, .sqlx regenerated; 288 tests green; critique CLEAN. Ship agents failed to confirm Copilot requests → conductor verified/requested directly. NEW ENGINEER FLAGS (queued): (1) **placement changelog variant missing from the Changelog DD** though 87 AC1 names placement changes — 70 will emit none + flag; amend DD or bless the variant; (2) append-only DB trigger = beyond-ruling depth, ratify or drop; (3) ChannelPointer cap = 512 (named const) + note text has no length cap (Title precedent) — ratify. Conductor ops: fixed worktree `.env` missing ZURFUR_PDS_IMAGE (worktree-init seeding gap — file a fix) + started the s0 compose stack. Wave B launching: S1+S2+S4 parallel off 87-branch, S3 forking at S1@72.

- **2026-07-05 ~19:05 — Gate A batch-2 RESOLVED (Engineer returned mid-wave-A).** All 9 holds ruled via 3-round interview: B2 persisted participants ✓rec, **B1 FLIPPED — Commission Admin real in v1** (honors Structural Authority DD 29425666; reverses the afternoon's 93/83 rulings — Jira corrected: 93 annotation rev-2 + comments 10041/10042; shared `is_owner||has_role(Admin)` helper strategy, role arm inert until 83, 83 sweeps earlier sites incl. 87 channel + 70 grants), B6 matrix ✓rec, B4 ceiling-wins ✓rec, B3 widen-75 ✓rec, **B5 PAID-mints-fact (against rec — Ask-for-Art governs; Invoices DD amendment queued)**, B8 wire-blob-deletion ✓rec, B9 terminal ✓rec, B7 extend-91 ✓rec. Comments 10041–10050 posted. ALL STACKS UNBLOCKED. Wave B briefs must carry: B1-flip helper pattern, B2 table in 76, widened 75 ACs + full effective() formula, B5 paid-keying, B6 matrix, B8 blob-delete in 66.

- **2026-07-05 ~19:15 — Unit opened; wave A launched.** Prior unit 28ca4f verified closed (ledger preserved at `logbooks/28ca4f-final-ledger.json`). Epic expanded (34 children), 9 Engineer forks ruled via batched interview (all recommended options; recorded to Jira with explicit approval — comments 10031–10039 + AC corrections on 31/93/94). Scout workflow `wf_0eae4cb5-d62` (6 scouts + Opus pre-gate, ~1.1M tokens): verdict **proceed**; ZMVP-65 found already merged; S1 functionally independent of S0 (kept stacked for S3 lineage); E1–E35 seam rulings triaged into adopted/ruled/holds (gate-a doc). Map v2 recorded. Engineer away — away-protocol active (build all unblocked, batch forks). Wave A: S0 spine (67 → 87) building in `~/code/zurfur-zmvp18-s0`; per-ticket PR + Copilot; wave B (S1/S2/S4 + S3@77) launches off S0-top on completion.
