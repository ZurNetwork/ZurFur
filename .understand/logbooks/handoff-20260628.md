# Unit handoff — planning + decisions (2026-06-28, end of session)

**Status:** PLANNING COMPLETE, decisions settled & recorded. No build started (no branches/worktrees). uow not yet minted — `/parallelize` mints it tomorrow.

**NEXT ACTION (resume here):** `/parallelize ZMVP-26 ZMVP-38` → it records the ledger (mints uow), runs `/close-gaps --pre` on the cached briefings, then fans out the build. Reuse the cached snapshots via `/remember` (paths below). **Do not re-run `/next-path` and do not re-litigate the "defer" verdicts** — the briefing agents' blockers were stale and have been overturned (see below).

---

## Open threads (what's not yet sound / loose ends)
- [ ] **Build the approved set:** ZMVP-26 (OpenAPI `/plugin/v1` spec) ∥ ZMVP-38 (Collection lexicon). Disjoint footprints, both Claude-takeable, neither touches the parked router (ZMVP-39) or adapter-pg writes (ZMVP-36).
- [ ] **ZMVP-26 caveat:** author the fully-settled surface (auth=app_key bearer, errors=RFC 9457, versioning, CSRF-exempt) now; **stub Commission resource bodies as `$ref`s pending ZMVP-19** (Commission schema not yet pinned). Confirm this stub approach is acceptable before shipping.
- [ ] **ZMVP-38:** Referenceable v1 = **Account + User only** (decided). Author the lexicon generic over member kind with just those two; Character/Post are a later backward-compatible enum addition.
- [ ] **ZMVP-30 re-scoped** by the new handle DD (24870914): onboarding is now interactive handle-choice (suggest→normalize→availability→provision) behind a hard write-gate, NOT a silent mint. Infra half split to a **new ticket** (created this session — see ledger/Jira). ZMVP-30 is a *follow-on*, not in the immediate set.
- [ ] **DD open items** (from 24870914): Zurfur reserved-label list; punycode/confusable policy for brought domains; Account-handle change flow vs user handle-swap (independent — confirm + document).
- [ ] **Stale Jira statuses** (quick cleanup, not yet done — needs a nod): ZMVP-21/23/40 show "In Review", ZMVP-24 "In Progress", but all four are MERGED (#69/#70/#71/#68). Offer to transition them to Done.
- [ ] **Deferred tickets** still parked: ZMVP-36 (UoW — open Engineer fork: cross-aggregate vs per-aggregate granularity), ZMVP-39 (router split — now UNBLOCKED since its colliding tickets merged; mechanical; high-leverage solo unit), ZMVP-37 (Private Collections — out of v1 scope per Collection DD), ZMVP-34 (delete Account — design-complete but heavy atproto/Group + collisions).

## Decisions made this session (recorded in Confluence)
1. **Default Account handle is user-chosen at onboarding** (not auto-derived). Either a Zurfur-issued `<label>.zurfur.app` subdomain or a brought domain; same atproto resolution mechanism (DNS `_atproto` TXT / well-known + bidirectional `alsoKnownAs`); Account `did:plc` Zurfur-operated either way; hard write-gate; suggested overridable default label; atproto-spec normalization + reserved labels. → **DD page 24870914** "The Default Account's Handle".
2. **Referenceable v1 coverage = Account + User only** (Character/Post deferred). → recorded on the Collection DD (24182787) "Open / follow-up" table.

## Why we're here (terse trail)
- Resumed: prior unit 7ff9c7 was COMPLETE (ZMVP-24/21/23/40 merged #68–71). Fell through to `/next-path`.
- Fanned out `/understand` on 5 candidates (26/34/30/38/37). All 5 briefings said "defer" — but several cited **design forks that were actually already settled in Confluence** (the agents asserted blockers without checking the source of truth).
- Engineer flagged this ("check the Confluence — we settled Plugin-security and others"). Fanned out 3 Confluence audits → confirmed: ZMVP-26's "triple-block" was stale (REST/OpenAPI + app_key→token + RFC 9457 all DECIDED in DD 24543244 / 23592962); the Collection Lens fork was stale (pinned on Lens page 10453000); ZMVP-37 is cleanly out-of-v1-scope; ZMVP-34 fully design-settled.
- Only TWO genuine small decisions remained → both made (above) → handle one grew into a full DD.

## Cached briefings (reuse via /remember — but treat the "defer" verdicts as SUPERSEDED)
- ZMVP-26: `.understand/20260628-232209_ZMVP-26_plugin-v1-openapi-spec.md`
- ZMVP-38: `.understand/20260628-232342_ZMVP-38_collection-lexicon.md`
- ZMVP-30: `.understand/20260628-232316_ZMVP-30_default-account-provisioning.md`
- ZMVP-34: `.understand/20260628-232445_ZMVP-34_owner-deletes-account.md`
- ZMVP-37: `.understand/20260628-232318_ZMVP-37_private-index-collections.md`

## Key Confluence refs
- DD "The Default Account's Handle" — https://zurnetwork.atlassian.net/wiki/spaces/DESIGN/pages/24870914
- Collection DD (Referenceable v1 row) — page 24182787
- Account glossary — page 1966081 · Onboarding 1DD — 21594113
- Plugin/Auth DD — 24543244 · Error-model DD — 23592962 · Lens — 10453000
- cloudId: cafe5eef-9c51-4800-85df-ef42187f9414
