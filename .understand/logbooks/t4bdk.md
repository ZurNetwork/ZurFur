# Logbook — uow `t4bdk`

> Unit: ZMVP-36 (Unit of Work) ∥ ZMVP-48+ZMVP-45 (Handle validation newtype). Ledger: `.understand/parallel-set.json`. Minted 2026-06-30.

## Open threads

- [ ] **DD 24150017 is PROPOSED, not DECIDED** — offer `/design-sync` to flip it once ZMVP-36 lands.
- [ ] **DD 24870914 open items** (punycode + reserved-label) — offer `/design-sync` to close once the Handle unit lands.
- [ ] **ZMVP-36 guard exceptions = TWO sites, not one** — `session_store.rs` (`tower_sessions::SessionStore`) AND `adapter-atproto/src/auth_store.rs` (`jacquard_oauth::ClientAuthStore`, 4× bare-pool writes), both external `&self` traits with no transactional home. Build treated both as documented exemptions. **Fold `auth_store` into DD 24150017's "documented exceptions" list during `/design-sync`** (briefing only named session_store).
- [ ] **ZMVP-36 profile-cache write on the GET /me READ path** — `resolve_profile` now opens a write UoW for the best-effort cache fill (errors swallowed). Works, but it's the one read endpoint opening a write tx. Engineer may prefer making `profile` an exemption like session_store/auth_store instead. Revisit at post-gate/ship.
- [ ] **ZMVP-36 impl risks to verify-first** — dyn-safe accessor view `Box<dyn AccountWrites + '_>` + consuming `commit(self: Box<Self>)` under `#[async_trait]`. Settle in the first build hour before the mechanical sweep.
- [ ] **Handle RFC-9457 claim-site wiring HANDED OFF** — no handle-accepting endpoint exists (`POST /accounts` takes only `name`). `HandleError → Problem` mapping + integration tests deferred to ZMVP-44/30 as an `#[ignore]`d test + note. Don't lose this.
- [ ] **External-worktree merge overlap** — `feature/openapi-infra` may touch account routes (↔ ZMVP-36 handler rewrite); `feature/characters-domain` may touch `domain/ports.rs` + `elements.rs` (↔ both units). Merge-time only; watch at `/prepare-pr` rebase.
- [ ] **CI guard mechanism undecided** — clippy `disallowed-methods` (needs `clippy.toml`) vs one-line grep test. Pick at build; DD left it open.

## Next domain lane (Engineer)

- **Rotation-key custody & real `did:plc` minter** — the remaining ZMVP-44 blocker after the resolution fork (DD 26607618) closed. Who holds the PLC rotation key, does the user co-hold one (portability off Zurfur), run vs Bluesky's `plc.directory`; gates the `alsoKnownAs` write (outbox dual-write) and the real minter (today `StubDidMinter`). DD 4358151 lists this as a "separate infra DD, in review." Touches DID/handle correlation + credential custody → needs `/security-review`. **Teed up as Zuri's next lane — say the word and I'll fan out a cited investigation like the resolution fork.**

## For the retrospective

- **Two force-push incidents on live branches, both caught & recovered, no work lost — but a fragility pattern.** (1) Handle ship-agent hit the already-merged-and-deleted #77 branch on `--force-with-lease` (stale lease) → stopped correctly. (2) ZMVP-36 fix-agent's first push clobbered `e4bd3c8` (a main-merge that had integrated Handle #77 into the #78 branch externally), dropping it; caught from push output, recovered via `reset --hard e4bd3c8` + cherry-pick + re-gate. **Lesson:** before force-pushing to a LIVE/open PR branch, `git fetch` and reconcile — the branch may have been merged, branch-updated, or reverted externally; key `--force-with-lease` to the freshly-fetched remote ref, not a stale local one. Candidate for a `feedback` memory — let the retro decide.

## Entries

- **2026-06-30 15:15 — uow minted, set recorded.** Re-planned after ZMVP-39 (#76) merged and freed the api crate; the prior unit (h0rnz) had declared a "decision wall" that was partly an artifact of ZMVP-39 owning `lib.rs`. Scout confirmed: handle validation greenfield, UoW unimplemented (bare-pool writes), Collection lexicon-only, account-deletion atproto half stub-only. Two genuinely-parallel settled-DD foundations selected. Engineer disposed all forks at planning (set approved; UoW shape = single aggregate-neutral factory w/ accessor views per DD 24150017 §7; migration scope = all-pg-one-pass; Handle = bootstrap now, reserved-labels *.zurfur.app-only, starter set approved). → ledger.
- **2026-06-30 — next:** `/close-gaps --pre`, then fan-out `/start`+`/implement` per worktree.
- **2026-06-30 15:18 — `/close-gaps --pre` PASSED.** Set coherent; the two units are genuinely disjoint even within the `domain` crate (ports.rs vs new elements/handle.rs). Builds fanned out (agents a45438c0c627aefa5 ZMVP-36, a650b1b6f20d730a2 Handle).
- **2026-06-30 15:35 — Handle(48+45) BUILT & green.** Two files only (`domain/src/elements/handle.rs` + `elements.rs` reg); 19 unit + 3 doctests green, fmt/clippy clean. Verify-first done: fetched live DD 24870914 §6, rules matched (no correction). BYO `api.example.com` accepted; `*.zurfur.app` reserved labels rejected. RFC-9457 mapping deferred → `#[ignore]`d test naming ZMVP-44/30. Phase→built. Ship tail held for `/close-gaps --post` (needs ZMVP-36 too).
- **2026-06-30 16:40 — ZMVP-36 BUILT & all green.** 24 files +1406/−1023; new `adapter-pg/src/uow.rs` + `adapter-pg/tests/no_bare_pool_writes.rs`. `cargo test --workspace` 93 passed/0 failed; fmt+clippy(-D warnings) clean; CI guard verified to actually FAIL on a planted `.execute(&self.pool)`. dyn-safe `commit(self: Box<Self>)` + accessor views worked, no fallback. Built exactly to DD 24150017. Nothing handed off. Flags → open threads (auth_store 2nd exception; profile-on-read). Phase→built. **Both units now built → `/close-gaps --post` next.**
- **2026-06-30 17:45 — ZMVP-48+45 MERGED (#77 → main 53036cc) + Jira Done.** Merge happened mid-review-fix (by ProgrammingCheetah) so the Copilot doc cleanups (commit 622ad7b) missed the squash; branch auto-deleted. A stray `revert-77` branch appeared briefly (full revert of the feature) with no PR — **Engineer deleted it; feature stays on main.** Cleanups re-landed as follow-up **PR #80** (`bug/zmvp-48-handle-doc-cleanup`, cherry-pick of 622ad7b off current main, gates green). Handle worktree retained until #80 merges. Lesson: a merge can land while review-fixes are still in flight — reconcile remote state before force-pushing (the ship agent correctly stopped instead of recreating a dead branch).
- **2026-06-30 16:41 — [PARALLEL DOMAIN LANE] DD recorded: Handle Resolution = HTTPS well-known.** Zuri decided ZMVP-44 resolution fork via `/design-decision` (3-agent cited investigation: spec + Bluesky prior art + ops/security). → Confluence DD **26607618**, ZMVP-44 comment, memory `project_handle_resolution_wellknown` + index + design-index. Resolution wall item CLOSED; key-custody/minter fork still open (next domain lane). This is the new "carve the Engineer a forward-leveraged domain lane" rule working: Zuri unblocked ZMVP-44's serving half while Claude's mechanical units built.
