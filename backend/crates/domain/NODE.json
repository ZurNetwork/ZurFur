{
  "path": "backend/crates/domain",
  "charted": "2026-09-17",
  "short": "The I/O-free domain core",
  "is": "Zurfur's I/O-free core — the platform's nouns and the trait seams every adapter implements; deliberately transitional, to split into per-namespace crates (identity, gallery, workflow, plugin).",
  "conventions": [
    "Every element mirrors a design glossary page (`entities/` in the corpus); the page pointer lives in this node's `refs`, never in a doc comment.",
    "Stub modules are allowed, and documented honestly as stubs.",
    "Errors surface as `anyhow::Result` with context."
  ],
  "entry_points": [
    "src/lib.rs"
  ],
  "fs": [
    {
      "name": "Cargo.toml",
      "role": "value-object deps: chrono, uuid, cid, serde, zeroize, async-trait; tokio io-util only to name AsyncRead for the streaming FileStore port",
      "node": false
    },
    {
      "name": "README.md",
      "role": "architecture overview: elements/ports/datetime, principles, usage",
      "node": false
    },
    {
      "name": "src/",
      "role": "elements + ports + the clock + the string builder",
      "node": true
    },
    {
      "name": "tests/",
      "role": "fact_contract.rs — the Fact contract (commission-anchored evidence blocks hard deletion); elements_do_not_import_ports.rs — dependency-direction guard (elements never depend on ports)",
      "node": false
    }
  ],
  "refs": [
    {
      "page": 3014657,
      "title": "Deletion of Commissions",
      "governs": "tests/fact_contract.rs: why no production Fact implementor exists yet"
    },
    {
      "page": 34013187,
      "title": "Identities — the Actor Super-Table, Kind-Checked References & the Polymorphism Ban",
      "governs": "Actor super-table"
    },
    {
      "page": 45514754,
      "title": "Commission Composition — Surfaces as Extension Points, Flat Elements and Tabs",
      "governs": "flat composition"
    },
    {
      "page": 64290818,
      "title": "Libraries as Composition — the Approved Crate Set and the Newtype Derive Rules",
      "governs": "newtype derive rules: path-qualified derives, thiserror owns errors, Deref dropped, FromStr/From derived only where the inner type carries the rule, IdError retired; strum allowed here exact-pinned with token tests"
    }
  ],
  "notes": [
    "See also: the design corpus glossary — `entities/` in ZurNetwork/zurfur-design, indexed by `docs/design-index.md`.",
    "See also: ZMVP-67 (tests/fact_contract.rs: the Fact contract's origin ticket)."
  ]
}
